Execute the approach discussed above. Plan restrictions are removed and the previously active tool set is restored.

Continue with implementation and verification only when the user explicitly requested execution. If an approved `<proposed_plan>` is included below, treat it as the source of user intent, re-read files as needed, and complete its steps in order.

Keep changes focused, avoid unrelated refactors, and validate according to the risk of the change. If additional user instructions are provided below, treat them as the latest authority.
